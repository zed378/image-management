# Copy to .env for local development. .env is git-ignored.
# Every variable the services read is listed here; P0-04 validates them at
# startup and refuses to boot on a missing or invalid value.

NODE_ENV=development
LOG_LEVEL=info
