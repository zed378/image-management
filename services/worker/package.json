{
  "name": "@image-delivery/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Queue consumers: derivative generation, webhooks, usage, sweepers (ADR-017).",
  "scripts": {
    "build": "tsup",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "@image-delivery/config": "workspace:*",
    "zod": "^4.6.5"
  }
}
